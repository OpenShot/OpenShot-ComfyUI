# Keep dependencies minimal and aligned with common Comfy installs.
numpy>=1.24
Pillow>=9.0
hydra-core>=1.3
decord
transformers>=4.39
timm>=0.9

# IMPORTANT:
# SAM2 is intentionally NOT installed from this file to avoid repeated
# large git downloads during normal dependency updates.
# Ensure your Comfy environment already has a compatible `sam2` installed.
