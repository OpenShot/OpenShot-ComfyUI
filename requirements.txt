# Keep dependencies minimal and aligned with common Comfy installs.
numpy>=1.24
Pillow>=9.0
hydra-core>=1.3
# SAM2 upstream package
# NOTE: this may pull torch-related dependencies depending on environment.
git+https://github.com/facebookresearch/sam2.git
