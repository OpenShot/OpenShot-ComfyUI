# Keep dependencies minimal and aligned with common Comfy installs.
numpy>=1.24
Pillow>=9.0
hydra-core>=1.3
# `sam2` is expected to be available in the Comfy runtime.
# If missing, install from Meta SAM2 sources compatible with your Comfy environment.
